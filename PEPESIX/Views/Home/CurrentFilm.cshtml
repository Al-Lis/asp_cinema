@{
    ViewData["Title"] = "Sessions Page";
}
<div >
    @foreach (var item in ViewBag.Sessions)

    {
        <div class="card border-light mb-3" style="max-width: 220rem;">
            <section>

                <div style="margin-left:1%; margin-right:1%; margin-top:1%; margin-bottom:1%">
                    <div class="card-header" onclick="GetSessionById(@item.SessionId)"><strong>Сеанс</strong></div>
                <h4 class="card-title">Дата и время сеанса: @item.DateTime.ToString("dd/MM/yyyy HH:mm")</h4>
                <p class="card-text">Цена: @item.Price.ToString("0")</p>
                </div>
            </section>
        </div>
    }
</div>


    <style>
        .card-header{
            font-size: 16pt;
        }
        .card {
            background-color: transparent;
            text-shadow: 0 0 1px rgba(255, 255, 255, 0.3), 0 0 2px rgba(255, 255, 255, 0.3), 0 0 5px rgba(255, 255, 255, 0.2);
        }

        .mb-3 {
            margin-bottom: 1rem !important;
        }

        .border-light {
            --bs-border-opacity: 1;
            border-color: rgba(var(--bs-light-rgb), var(--bs-border-opacity)) !important;
        }

        .card.border-light {
            box-shadow: 0 0 2px rgba(68, 217, 232, 0.9), 0 0 4px rgba(68, 217, 232, 0.4), 0 0 1rem rgba(68, 217, 232, 0.3) 0 0 4rem rgba(68, 217, 232, 0.1);
           
    }
    </style>




@*<div >
    @foreach (var item in ViewBag.Sessions)
    {
    <div class="card bg-info mb-3" style="max-width: 320rem;">
        
        <section>
            <div style="margin-left:1%; margin-right:1%;">
                    <div class="card-header" >
                    <h4>@item.DateTime.ToString("dd/MM/yyyy HH:mm")
                             <strong style="font-size: 24px; margin-left:10px; color: #6f42c1; ">@item.Price.ToString("0")</strong>
                             
                            </h4>
                </div>

                <div class="film__card">
                    <div class="film__card--img">
                        <img src="~/Images/@item.Film"  id="ph" style="margin-right: 30px" />
                    </div>
                    <div class="film__card--info">
                        <div class="card-body film__card--info-description">
                            <p class="card-text" style="min-width: 200px; font-size:21px;">@item.NumberZal </p> 
                        </div>
                       
                    </div>
                </div>
            </div>
        </section>
    </div>
    }
</div>*@
<script>
    function GetSessionById(id) {
        $.ajax({
            url: '@Url.Action("GetSessionById", "Home")' + '/' + id,
            type: 'GET',
            success: function(data) {
                    console.log(data)
                    window.location.href = '@Url.Action("Zal","Home")' + "/" + id            
            },
            error: function(error) {
                console.log(error)
            }
        });
    }
</script>
